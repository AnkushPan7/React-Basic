import React from 'react';

const Footer = () => {
  return (
    <footer>
      <button style={{ backgroundColor: 'blue', color: 'white', textTransform: 'uppercase' }}>LOGOUT</button>
    </footer>
  );
};

export default Footer;