import React from 'react';

/**
 * TestComponent serves as a placeholder component for testing purposes.
 * It renders a simple message to verify the frontend setup.
 *
 * @returns {JSX.Element} A div element containing a test message.
 */
const TestComponent = () => {
  return (
    <div>
      <h1>Test Component Rendered</h1>
      <p>This component is a placeholder for testing purposes.</p>
    </div>
  );
};

export default TestComponent;