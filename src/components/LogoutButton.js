// Assuming the LogoutButton component is in LogoutButton.js
import React from 'react';

const LogoutButton = () => {
  return (
    <div>
      <button>Logout</button>
      <button style={{ backgroundColor: 'green' }}>Hello</button>
    </div>
  );
};

export default LogoutButton;