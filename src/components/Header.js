import React from 'react';
import './Header.css'; // Assuming you have a CSS file for styling

function Header() {
  return (
    <header className="header">
      {/* Other header content */}
      <button className="add-form-button">ADD FORM</button>
    </header>
  );
}

export default Header;