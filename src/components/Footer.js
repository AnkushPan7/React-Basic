// Example Footer.js content (adjust path and content as needed)
import React from 'react';

function Footer() {
  return (
    <footer>
      <button onClick={() => console.log('Logout clicked')}>Logout</button>
    </footer>
  );
}

export default Footer;